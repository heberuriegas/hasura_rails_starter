table:
  name: users
  schema: public
remote_relationships:
- definition:
    hasura_fields:
    - id
    remote_field:
      file:
        arguments:
          input:
            recordId: $id
            recordType: User
    remote_schema: Backend
  name: avatar
